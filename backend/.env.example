# Copy this file to .env and fill in the values for local development.
# .env is gitignored — never commit real keys.

# Google Maps Platform key. Must have the "Distance Matrix API" enabled.
# Backend uses this for server-side distance calculation on cargo creation.
GOOGLE_MAPS_API_KEY=
